import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#102116",
    flex: 1,
    padding: 16,
  },
  iconRow: {
    flexDirection: "row",
    flexWrap: "wrap",
    marginTop: 8,
  },
});
